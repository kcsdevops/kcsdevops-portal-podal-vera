# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/refs/heads/main/schemas/v1.0/azure.yaml.json

name: podal-vera-agendamento

infra:
  provider: bicep

services:
  backend:
    project: backend
    host: containerapp
    language: ts
    docker:
      path: Dockerfile
      context: ./
